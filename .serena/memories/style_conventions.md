Current Python codebase has almost no conventions established yet. Existing package uses Python 3.12+, minimal typed function style, snake_case naming, and no docstrings in the scaffold. For upcoming work, preserve type safety, prefer dataclasses or Pydantic models for structured catalog/config data, keep CLI/service/repository layers separated, avoid broad exception swallowing, and preserve behavior-centric parity with the legacy PowerShell script. The legacy script is data-heavy; its catalogs/themes/profiles/providers should be externalized into TOML/JSON or fine-grained Python modules rather than kept in a monolithic command file. Terminal UX should preserve glyph-rich output with a clean ASCII fallback.
