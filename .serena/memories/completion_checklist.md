When implementation work is done, verify at minimum: package metadata updated; console entrypoint works; legacy PowerShell parity for selected commands is covered by tests; glyph/ASCII rendering behavior is validated; config/catalog loading is validated; repo-profile autodetection works; CI workflow passes lint/test/build; packaging builds via uv build; TestPyPI/PyPI workflow files are consistent with chosen release policy; documentation explains commands, config format, and submodule strategy.
