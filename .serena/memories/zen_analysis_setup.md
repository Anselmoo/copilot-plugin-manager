# Zen Code Quality Analysis
Project onboarded for zen-of-languages analysis.

Supported languages: Rust, Python, TOML
- Use `mcp_zen-of-langua_analyze_batch_auto` for smart pagination
- Use `mcp_zen-of-langua_analyze_batch_summary` for quick health check
- Use `mcp_zen-of-langua_analyze_zen_violations` for snippet analysis

Project configured for architectural pattern detection and code quality scoring.
